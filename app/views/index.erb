<h1>All Tasks</h1>

<table class="table table-hover" >
  <tr>
    <th>ID</th>
    <th>Task</th>
  </tr>

  <% @tasks.each do |task| %>
  <tr>
    <td> <%= task.id %>: </td>
    <td> <a href="/tasks/<%= task.id %>"><%= task.title %> </a></td>
  </tr>
  <% end %>
</table>
